#include <iostream>
#include <vector>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    int n, cur_max, length = 0;
    int i;
    scanf("%d", &n);
    vector<int> arr(n, 0);
    for(i = 0; i < n; i++)
        scanf(" %d", &arr[i]);

    // solving
    
    return 0;
}