// Baekjoon No. 11053 - 가장 긴 증가하는 부분 수열
// Time Complexity
// #DP

#include <iostream>
#include <vector>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n;
    int i;
    cin >> n;
    vector<int> arr(n, 0);
    vector<int> memory(n, 0);
    for (i = 0; i < n; i++) {

    }

    return 0;
}
