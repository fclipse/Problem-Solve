#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

using namespace std;

int main() {
	fastio;
    string str;
    int i;
    getline(cin, str);
    cin >> i;
    cout << str[i - 1];

	return 0;
}