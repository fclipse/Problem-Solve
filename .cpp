#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define NUM 1000000

using namespace std;

// 각 숫자별 이제까지 나눠온 횟수 저장
int divCnt[NUM] = { 0 };
int divHistory[NUM][100] = { 0 };

int main() {
	fastio;

	int n;
	cin >> n;
	for (int i = NUM; i > 0; i--) {
		if (i % 3 == 0) {

		}
	}

	return 0;
}